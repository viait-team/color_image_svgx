sudo apt update
sudo apt install -y \
  build-essential libssl-dev zlib1g-dev libbz2-dev \
  libreadline-dev libsqlite3-dev curl libncursesw5-dev \
  xz-utils tk-dev libxml2-dev libxmlsec1-dev libffi-dev liblzma-dev


sudo apt install pngquant pngnq imagemagick potrace

which pngquant
which pngnq
which convert
which identify
which potrace

(1) re-install imagemagick

sudo apt purge imagemagick
sudo apt install build-essential checkinstall
git clone https://github.com/ImageMagick/ImageMagick.git
cd ImageMagick
./configure
make
sudo checkinstall

convert -version

(2) re-install tersseract-ocr

sudo apt update
sudo apt install -y libpango1.0-dev libcairo2-dev libpangoft2-1.0-0 libpangocairo-1.0-0 \
  libfontconfig1-dev libicu-dev
  
cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/usr/local -DBUILD_TRAINING_TOOLS=OFF ..
  
# clone tesseract
git clone https://github.com/tesseract-ocr/tesseract.git
cd tesseract
git fetch --all --tags
git checkout 5.3.0

# build with CMake (recommended)
mkdir build && cd build
cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/usr/local ..
make -j"$(nproc)"
sudo make install
sudo ldconfigmkdir 



sudo apt purge tesseract-ocr
sudo apt install libleptonica-dev
git clone https://github.com/tesseract-ocr/tesseract.git
cd tesseract
git checkout 5.3.0  # or latest stable
./autogen.sh
./configure
make
sudo make install

tesseract --version

(3) re-install pngquant

sudo apt update
sudo apt install -y build-essential git libpng-dev

# Clone the latest stable version of pngquant
git clone --recurse-submodules https://github.com/kornelski/pngquant.git

# Navigate into the source directory
cd pngquant

# Compile the source code
./configure
make

# Install the compiled binary to your system
sudo make install


sudo apt purge pngquant
git clone --depth 1 --branch 2.17.0 https://github.com/kornelski/pngquant.git
cd pngquant
make
sudo make install

pngquant --version


(4) 

pip install opencv-python

pip install svg-stack

pip install lxml


(5) tersseract diff

 Linux 22.04


chao@FTS-Dev-AI:~/AllProjects/image-svgx/color_image_svgx$ tesseract --version
tesseract 5.3.0
 leptonica-1.82.0
  libgif 5.1.9 : libjpeg 8d (libjpeg-turbo 2.1.1) : libpng 1.6.37 : libtiff 4.3.0 : zlib 1.2.11 : libwebp 1.2.2 : libopenjp2 2.4.0
 Found AVX2
 Found AVX
 Found FMA
 Found SSE4.1