{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open current HTML file",
            "file": "${file}"
        }
    ]
}